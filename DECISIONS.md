# 설계 기록 — 업무 리듬 봇

2026-09-09 세션에서 빈 폴더부터 동작하는 봇까지 만든 기록.
**왜 그렇게 했는지**를 남긴다. 무엇을 하는지는 `README.md`, 어떻게 하는지는 코드에 있다.

---

## 1. 요구사항

사용자가 세션 중에 준 것을 시간순으로. 뒤로 갈수록 구체화됐다.

1. 사람들이 업무를 **체계적·지속적으로** 하도록 돕는 슬랙 앱
2. **취지** — 퇴직한 사람이 이 앱이 들어간 채널에서 **직장 다니는 것과 비슷한 패턴으로** 계속 일하게 하는 것
3. 매일 **17:50** done-next 질문
4. 슬래시 명령 — 업무보고, 계획관리, 마일스톤 설정·진행도 갱신
5. 규모가 크면 앱을 쪼개도 됨
6. **done-next 는 대화형** — 회사에서 쓰는 봇의 실제 대화를 예시로 제시
   - Q1 오늘 한 일 (+ 지난 보고의 NEXT 인용)
   - Q2 내일 할 일
   - Q3 "오늘 하루도 고생한 나에게 해주고 싶은 말은?" ← 개인 요구사항으로 추가
7. **아침 9시** 에 "오늘 뭘 하기로 했는지" 먼저 언급
8. `/출근` `/퇴근` — 일간·주간·월간 근무시간, **점심 1시간 제외**
9. 완료 메시지 문구 지정 (`:tada: Daily Retro/Planning 응답이 완료되었습니다!` …)
10. streak, 공개 채널 보드 포함
11. **두 채널 분리** — 개인 기록은 봇 DM(`Manager oh`), 개인별 보고는 `#0_daily-report`
12. 근무시간 기본값 — 미출근 0시간, 출근했으면 8시간
13. 답변을 할 일로 쪼개는 규칙 개선
14. **근무 시작에 "DM 에 아카이빙해 둔 자료 확인" 리마인드** — 신규 요구.
    요구사항 미팅에서 두 번 좁혀졌다 (§2 "아카이브 요구사항이 두 번 좁혀졌다").
15. 근무시간 기본 정책 확정 — **월~금 09:30~18:00, 점심 12:30~14:00**

---

## 2. 갈림길에서 고른 것

### 스택 (사용자 선택)

| 항목 | 선택 | 이유 |
|---|---|---|
| 호스팅 | **Railway** | 유료 크레딧 보유, 10명 미만이라 서버비 무시 가능 |
| 언어 | **TypeScript + Bolt** | Block Kit 타입이 있어 모달·버튼 오타가 컴파일에서 잡힘 |
| 저장소 | **SQLite** | 파일 하나. 백업이 `cp` 한 줄 |
| 트래커 연동 | **없음** | CollabOps 와 무관한 별도 프로젝트 |

### 슬랙 연결은 Socket Mode

공개 URL·도메인·서명 검증·3초 응답 제한을 전부 피한다.
Railway 에서 워커로 띄우면 끝이다. `SLACK_MODE=http` 로 전환 가능하게는 열어 뒀다.

### 앱은 하나, 코드에서 쪼갠다

슬랙 앱을 여러 개로 나누면 설치·토큰·권한을 사람이 여러 번 해야 한다.
마찰을 없애자는 취지에 정면으로 반한다.
대신 코드에서 `db / domain / slack / service / scheduler` 로 나눴고,
`domain/` 은 슬랙을 전혀 모른다 (그래서 `npm run smoke` 로 슬랙 없이 검증된다).

### done-next 는 모달이 아니라 대화

처음에는 모달로 짜다가 사용자가 실제 예시를 주고 나서 전부 갈아엎었다.

- **대화를 고른 이유** — 답이 DM 기록으로 남아 나중에 스스로 훑어볼 수 있다.
  모달은 제출하면 사라진다.
- 상태는 `standup_sessions` 테이블의 `stage`(q1/q2/q3/complete)로 관리한다.
  17:50 에 시작된 대화는 **답할 때까지 살아 있다.** 자정을 넘겨도 이어진다.

### 이 앱의 핵심은 "어제 하기로 한 일"

Q1 에 지난 보고의 NEXT 를 인용해서 같이 보여 준다.
아침 리마인드도 같은 재료를 쓴다.

> 빈 화면에서 기억을 짜내는 것과, 목록을 보고 고치는 것은 전혀 다른 일이다.

그래서 Q2 답변은 **다음 근무일의 할 일로 자동 등록**된다. 루프가 닫힌다.

### streak 은 근무일 기준

주말과 `/pause` 휴가는 건너뛴다.
쉬는 날 때문에 기록이 끊기면 **쉬는 것이 벌이 되고**, 그건 이 앱의 취지와 반대다.

### 마감 현황은 낸 사람만 호명한다

안 낸 사람 이름을 부르면 압박이 아니라 창피가 되고, 그러면 다음 날 아예 안 들어온다.
`아직 N명 남았습니다` 로만 적는다.

### 점심은 길이가 아니라 시각 구간

처음엔 "5시간 이상 일한 날에 60분 차감" 이었다.
정책이 **12:30~14:00** 으로 확정되면서 구간 교집합 방식으로 바꿨다.

| 근무 | 겹치는 점심 | 실 근무 |
|---|---|---|
| 09:30~18:00 | 90분 | 7시간 |
| 09:30~13:00 | 30분 | 3시간 |
| 09:30~12:00 | 0분 | 2시간 30분 |

오전 반차인 날에 1시간 30분을 빼면 근무시간이 음수에 가까워진다.
`computeWorked()` 가 tz 와 workday 를 받는 이유가 이것이다 — 점심 구간을 그날 그 지역 시각에 놓아야 한다.

### 아카이브 요구사항이 두 번 좁혀졌다

원문: *"매일 아침 근무 시작 시, 개인 슬랙 dm에 아카이빙해놓은 자료 확인이 필요합니다.
9:30 근무 시작 시간에 이를 리마인딩하는 알람을 받고 싶습니다"*

| 단계 | 만든 것 | 왜 좁혀졌나 |
|---|---|---|
| 1차 | 09:30 별도 알림 + `[내 DM 열기]` 딥링크 | self-DM 으로 오독 |
| 2차 | 09:00 에 합치고 어제 기록 전문 표시 | "봇과의 DM" 이 맞고, 내용이 DB 에 있음 |
| **3차 (확정)** | **모닝 루틴 알람 문구** | 요청자: *"dm을 읽어줄 필요 없고 그냥 알람처럼 리마인딩 메시지만 필요합니다"* |

**"9:30 알람" 도 철회됐다.** 출근 시각이 유연하니 별도 알람 대신
09:00 opening 메시지에 얹는 쪽으로 요청자가 동의했다.

최종 형태는 `COPY.morning.routine` 배열 세 줄이다. 로직이 아니라 문구다.

```
:sunrise: 오늘의 모닝 루틴
1. DM 에 아카이빙해 둔 자료 확인하기
2. 오늘 할 일 정하기
3. /출근 찍고 시작하기
```

2차에서 만든 어제 기록 표시는 남겨 뒀다. 요청자가 "필요 없다" 고 한 것이지
"빼 달라" 고 한 것은 아니고, 09:00 메시지는 원래도 어제 NEXT 를 보여 주고 있었다.
길다고 판단되면 `morningMessage()` 에서 `yesterdayDone` 한 줄만 빼면 된다.

### "개인 슬랙 DM 에 아카이빙해놓은 자료" 를 잘못 읽었다

요구사항 원문의 "개인 슬랙 DM" 을 처음에 **사용자의 self-DM**("나에게 보낸 메시지")로 읽고,
09:30 에 별도 알림 + `[내 DM 열기]` 딥링크를 만들었다.
봇 토큰으로는 남의 self-DM 을 읽을 수 없으니 링크로 내보낼 수밖에 없었다.

사용자가 되물어서 바로잡았다 — **"이 봇(Manager oh)과의 DM"** 이 맞다.
거기엔 done-next 답변과 `/done` 기록이 쌓이고, **내용이 전부 DB 에 있다.**
링크로 내보낼 이유가 없고, 본문에 그대로 담으면 된다.

그리고 그렇게 읽으면 **절반은 이미 구현돼 있었다** — 09:00 리마인드가 어제 NEXT 와
미완료 할 일을 이미 보여 주고 있었다. 빠진 건 어제 DONE 전문·Q3·마일스톤이었다.

결론: 09:30 알림을 폐기하고 09:00 한 통에 담는다.
아침에 30분 간격으로 두 번 울리는 것보다 낫다.

**교훈** — 남이 준 요구사항의 명사("개인 슬랙 DM")를 혼자 해석하고 만들지 말 것.
한 줄 물어보는 게 하루치 구현보다 싸다. 실제로 물어보니 요구는 두 번 더 작아졌다.

### 날짜는 문자열

사용자 타임존 기준 `YYYY-MM-DD` 로만 다룬다.
`Date` 객체를 들고 다니면 UTC 자정 경계에서 하루가 밀린다.

### 알림 중복 방지

`job_runs` 에 `(사용자, 잡, 날짜)` 를 원자적으로 INSERT 한다 (`INSERT OR IGNORE` 의 `changes`).
Railway 재배포로 프로세스가 다시 떠도 그날 알림이 두 번 가지 않는다.
프로세스가 죽어 있었으면 **2시간 안에서는 늦게라도** 보낸다.

---

## 3. 실제로 물린 함정

만들면서 진짜로 터진 것들. 다음 사람이 같은 데서 안 막히게.

### manifest `long_description` 최소 174자

137자로 냈다가 거절당했다. 243자로 늘려 통과.

### 토큰이 접두사만 있어도 부팅은 된다

`.env` 에 `SLACK_BOT_TOKEN=xoxb-` 만 있으면 config 검증을 통과하고
슬랙 호출에서야 `invalid_auth` 스택트레이스로 죽는다. 원인이 안 보인다.

→ `config.ts` 의 `requireToken()` 이 접두사·길이를 보고
**무엇을 어디서 받아오는지**까지 한국어로 알려 주게 했다.

### action_id 중복 → 뷰 전체가 거부된다 ⚠️

가장 오래 헤맨 것. 슬랙은 **한 화면 안에서 `action_id` 가 겹치면 뷰를 통째로 거부**한다.

```
`action_id` "act_report_period" already exists [json-pointer:view/blocks/6/elements/2/action_id]
```

홈 탭의 "오늘 보고서"·"주간 보고서" 버튼이 같은 id 였고,
할 일이나 마일스톤이 2개 이상이면 오버플로 메뉴도 같은 문제가 났다.

→ 항목마다 고유 id(`act_task_menu_12`)를 붙이고 핸들러를 `RegExp` 로 등록.
→ **`npm run smoke` 에 action_id 중복 검사를 넣었다.** 같은 실수가 다시 나면 테스트가 잡는다.

### 보드 채널이 "봇이 처음 발견한 채널" 이었다

동작은 했지만 우연에 기대고 있었다. 채널이 하나 더 늘면 흔들린다.

→ `BOARD_CHANNEL_ID` 환경변수로 못 박았다.
지정이 없고 채널이 둘 이상이면 부팅 로그에 경고를 남긴다.
`/settings` 로 개인이 고른 채널은 재시작이 덮어쓰지 않는다.

### 퇴근 미기록을 상한선으로 자르면 통계가 망가진다

처음엔 `MAX_SESSION_HOURS=14` 로 잘라서 잠깐 일한 날이 **13시간**으로 남았다.

→ **정규 퇴근 시각(`DEFAULT_CLOCK_OUT=18:00`)으로 마감**하게 바꿨다. 결과는 7시간.
   모르는 값을 크게 잡는 것보다 정규 근무일이 낫다.
→ 도움말에만 있고 없던 `/근무 수정` 을 실제로 구현했다.

### url 버튼도 ack 해야 한다

링크로 나가는 버튼도 슬랙은 `block_actions` 를 보낸다. 핸들러가 없으면 사용자 화면에 오류가 뜬다.
(지금은 url 버튼을 쓰는 곳이 없지만, 추가할 때 잊지 말 것.)

### Railway 배포 — 네이티브 모듈이 Node 버전을 붙잡는다

첫 배포가 `npm ci` 에서 죽었다.

```
prebuild-install warn install No prebuilt binaries found
  (target=24.10.0 runtime=node arch=x64 libc= platform=linux)
gyp ERR! find Python  Could not find any Python installation to use
```

Nixpacks 가 Node **24** 를 골랐는데 `better-sqlite3@11` 은 Node 24 용 prebuilt 가 없다.
소스 컴파일로 넘어갔고, Nixpacks 이미지에 Python 이 없어서 끝났다.

`engines.node` 가 `">=20"` 같은 **범위**였던 게 원인이다 — 범위를 주면 최신을 고른다.

→ `.nvmrc` 와 `engines.node` 를 **`22.x`** 로 고정.
   릴리스 자산 목록으로 확인함: `node-v127-linux-x64`(Node 22)는 있고 `v137`(Node 24)은 없다.

같은 로그에 지뢰가 하나 더 있었다. `npm warn config production` —
Railway 가 production 모드로 깔면 `typescript` 가 빠져서 그다음 `npm run build` 가 실패한다.
→ 빌드 명령을 `npm ci --include=dev && npm run build` 로.

그 둘을 고치니 install 단계는 통과했고, 이번엔 **빌드 단계**가 죽었다.

```
npm error EBUSY: resource busy or locked, rmdir '/app/node_modules/.cache'
```

`npm ci` 는 `node_modules` 를 통째로 지우는데, Nixpacks 는 `node_modules/.cache` 에
빌드 캐시를 **마운트**해 둔다. 마운트 지점은 rmdir 할 수 없다.
게다가 Nixpacks 가 install 단계에서 이미 다 깔아 놨으니 그 `npm ci` 는 중복이었다.

→ 빌드 명령을 **`npm install --include=dev --no-audit --no-fund && npm run build`** 로.
   `npm install` 은 있는 것을 두고 모자란 것만 채우므로 마운트를 안 건드린다.
   `.cache` 를 만들어 둔 상태에서 로컬로 재현해 확인함 — 디렉터리가 그대로 남는다.

**교훈** — 네이티브 의존성이 있으면 런타임 버전을 범위로 두지 말 것.
빌드 도구가 devDependencies 에 있으면 배포 플랫폼의 production 모드를 의심할 것.
그리고 **빌드 단계에서 `npm ci` 를 다시 돌리지 말 것** — install 단계가 이미 했고,
캐시 마운트와 충돌한다.

### 입력은 0hop, 취합은 1hop — 모달을 걷어낸 이유

운영해 보니 "오늘 할 일 3가지" 모달이 문제였다. 사용자 말 그대로:

> 채팅에 기록이 안 남아. 모달에 입력한건 어디서 다시볼지 몰라.

모달은 제출하면 사라진다. 내가 뭘 적었는지 되짚을 자리가 없다.
그래서 원칙을 세웠다.

| | 어디서 | 예 |
|---|---|---|
| **입력 (0hop)** | 입력한 그 자리 = DM 대화 | 오늘 할 일, done-next, 출근·퇴근, `/done` |
| **취합 (1hop)** | 명령으로 여는 별도 뷰 | `/plan` `/report` `/milestone` `/settings` |

`checkinModal` 을 지우고 `checkin_sessions` + `checkinFlow` 로 바꿨다.
done-next 와 같은 구조다 — 봇이 묻고, 사용자가 평범한 메시지로 답하고, 봇이 저장 내용을 되읽어 준다.

마일스톤 연결은 select 박스 대신 **줄 끝의 `#번호`** 로 받는다. 위젯 없이 대화 안에서 끝난다.

### 슬랙 응답 방식 세 가지가 전부 기록을 지운다

같은 뿌리에서 나온 버그 세 개를 한꺼번에 고쳤다.

| 방식 | 무슨 일이 나나 |
|---|---|
| 모달 | 제출하면 사라진다 |
| `response_url` 응답 | **원본 메시지를 교체한다** (기본값이 `replace_original: true`) |
| 임시(ephemeral) 응답 | 새로고침하면 사라진다 |

두 번째가 특히 고약했다. 아침 리마인드에서 `[출근]` 을 누르면 그 리마인드가 통째로
"출근 09:07" 로 바뀌었다. 사용자 지적:

> 출근 버튼 누르면 메시지 자체가 변해. 메시지가 누적이 되어야 하는거아니야?

→ 액션 응답(`reply()`)을 전부 `chat.postMessage` 로 바꿨다.
   `/출근` `/퇴근` `/done` 도 DM 에 실제 메시지를 남기고,
   채널에서 부른 경우에만 그 자리에 짧게 덧붙인다.

고치고 나서 **같은 결함을 전수로 훑었더니 네 군데가 더 나왔다.**

| 지점 | 증상 |
|---|---|
| 할 일 오버플로 (완료·미루기·삭제) | **아무 반응이 없었다.** 홈 탭만 조용히 바뀜 |
| 마일스톤 오버플로 (되돌리기·보관) | 반응 없음 |
| `/pause` | 휴가를 잡아도 흔적이 안 남음 |
| `/milestone progress` · `done` | 진행도·완료를 바꿔도 흔적이 안 남음 |
| `/settings` 저장 | 무엇을 바꿨는지 확인할 자리가 없음 |

전부 DM 에 한 줄 기록을 남기게 했다.

**판정 기준** — 상태를 바꾸면 기록, 이미 있는 것을 모아 보여 주면 임시 응답.
지금 남아 있는 ephemeral 은 취합 뷰(`/plan` `/report` `/근무` `/streak`),
사용법 안내, "DM 으로 보냈습니다" 포인터뿐이다.

### 볼륨을 붙여도 DATABASE_PATH 를 안 고치면 데이터는 사라진다

재배포 후 하루치가 통째로 날아갔다. 볼륨은 `/data` 에 붙어 있었지만
`DATABASE_PATH` 가 기본값(`./data/worklife.sqlite`)이라 컨테이너 안 `/app/data/` 에 쌓이고 있었다.

증상이 애매했다 — "아침 질문이 또 왔다" 였는데, 이건 `job_runs` 까지 사라졌다는 뜻이었다.
멱등성 테이블이 날아가면 그날 알림이 다시 나간다.

→ 부팅할 때 **절대경로·기존 파일 여부·파일 크기·테이블별 행 수**를 로그에 남긴다.
   상대경로면 경고까지 띄운다. 로그만 봐도 볼륨을 쓰고 있는지 알 수 있다.
   호스팅 판정은 `RAILWAY_*` 변수로만 한다 — `PORT` 는 로컬에도 있어서 거짓 경보가 난다.

**교훈** — 영속성은 "볼륨을 붙였다" 로 끝나지 않는다. **쓰는 경로가 거기인지**까지 확인해야 하고,
그걸 사람이 기억하게 두지 말고 프로그램이 부팅할 때 말하게 해야 한다.

### 체크박스를 눌렀는데 네모가 그대로였다

`taskChecklist` 는 처음부터 완료면 `✅`, 계획이면 `⬜️` 를 그리고 있었다.
문제는 **누른 메시지를 다시 그리지 않은 것**이다. `/plan` 으로 띄운 목록에서
완료를 눌러도 그 메시지는 옛 상태 그대로 남았다. 홈 탭만 조용히 바뀌었다.

앞서 `response_url` 이 **원본 메시지를 교체**해서 아침 리마인드를 날려 먹은 적이 있어
그 경로를 통째로 걷어냈는데, 그러다 정당한 용도까지 같이 없앤 셈이었다.

**구분이 필요하다.**

| | 예 | 판정 |
|---|---|---|
| 다른 내용으로 덮어쓰기 | 아침 리마인드 → "출근 09:07" | ❌ 기록이 사라진다 |
| 같은 내용을 최신 상태로 다시 그리기 | 할 일 목록의 ⬜️ → ✅ | ✅ 갱신이다 |

→ `refreshMessage()` 를 따로 두고 할 일 액션에서만 쓴다.
→ `/plan` 과 액션이 **같은 `planView()`** 를 쓰게 했다. 그리는 코드가 갈라져 있으면 한쪽만 낡는다.
→ 완료·취소는 그 자리에서 보이므로 DM 알림을 뺐다. 미루기·삭제는 목록에서 사라지니 한 줄 남긴다.

### 회고 앱인데 지난 기간을 못 봤다

`/report` 가 오늘·이번주·이번달뿐이었다. `rangeFor()` 가 항상 `today` 로 경계를 계산해서
"지난달엔 뭐 했더라" 를 볼 방법이 아예 없었다. 회고가 목적인 앱에서는 구멍이다.

→ `parseRange()` 를 두고 `buildReport(user, range, today)` 로 바꿨다.
   `지난주` `지난달` `2026-09-05` `2026-08` `2026-09-01~2026-09-10` `30일` 을 받는다.
   `지난주` 는 `주` 를 품고 있어서 프리셋보다 **먼저** 검사해야 한다 — 순서가 곧 의미다.

→ `/history` 를 새로 뒀다. 구간 리포트가 "무엇을 했나" 라면 이건 "얼마나 꾸준했나" 다.
   **빈 날도 빼지 않는다** — 빠진 날이 보여야 리듬이 보인다.

→ 긴 기간은 슬랙 블록 3000자 제한에 걸린다. `파일` 키워드로 마크다운 파일 전송을 붙였다
   (`files:write` 필요, 없으면 그렇게 안내한다).

### 버튼 이름이 같은 뜻이면 기능이 달라도 소용없다

홈 탭에 `[퇴근하기]` 와 `[오늘 마무리하기]` 가 나란히 있었다. 하나는 타임카드,
하나는 회고 3문답인데 한국어로는 사실상 같은 말이다. 사용자가 바로 물었다.

→ `오늘 회고 쓰기` 로 바꿨다. 출근/퇴근 토글은 그대로 둔다 — 그건 헷갈리지 않는다.
→ 회고를 마쳤을 때 아직 퇴근 전이면 마무리 메시지에 `[퇴근 찍기]` 를 붙인다.
   저녁에 그 둘은 사실상 한 몸인데 서로 모르고 있었다.

### 살아 있는 화면과 확정된 보고서는 계산이 달라야 한다

홈 탭에 `11:35 출근 · 지금까지 1분` 과 `이번 주 근무 0시간` 이 같이 떴다.
`summarize()` 가 퇴근을 찍어 마감된 날만 셌기 때문이다. 틀린 값은 아니지만 대시보드로는 이상하다.

→ `summarize(..., { includeOpen: true })` 를 더했다. 홈 탭과 `/근무` 는 켜고,
   보고서는 끈다 — 보고서에는 확정된 값만 들어가야 한다.

### 마이그레이션은 되돌릴 수도 있어야 한다

09:30 알림을 접으면서 `users.archive_time` / `archive_reminder` 컬럼을 지워야 했다.
`ALTER TABLE ... DROP COLUMN` 은 두 번째 부팅에서 `no such column` 으로 터진다.

→ `db/index.ts` 의 오류 무시 패턴에 `no such column` 을 추가해서
   추가(`duplicate column`)와 삭제 양쪽 다 멱등하게 만들었다. 실제 DB 로 확인함.

### 답변 파싱 — 쉼표로 자르면 안 된다

사용자가 준 실제 done-next 답변이 근거였다.

```
웹훅 ERD + PRD 작성, 영상님과 미팅 (~70%)
```

두 항목이 아니라 **한 항목**이다. 한국어에서 쉼표는 항목 구분보다 문장 안 쉼표로 훨씬 자주 쓰인다.

함정 두 개를 더 막았다.
- `2.5시간 공부` 의 `2.` 를 목록 번호로 오인 → **숫자 기호는 뒤에 공백이 있을 때만** 인정
- URL 의 `/` → 구분자는 **앞뒤 공백을 요구**(` / `)해서 `https://a.com/b` 는 안 걸림

규칙은 `src/domain/textItems.ts` 한 곳에 있고, 표시와 할 일 생성이 같은 함수를 쓴다.
원문은 손대지 않고 그대로 저장된다.

---

## 4. 현재 상태

- 코드 44개 파일 · 약 5,200줄
- 타입체크·빌드·`npm run smoke`(검증 78건) 전부 통과
- **Railway 배포 완료 · 실제로 굴러가는 중**
- GitHub: `AvoChang/manageroh` (public — 워크스페이스 식별 정보는 올리지 않는다)

### 배포 상태 (2026-09-10 확인)

볼륨 영속성을 로그로 확인했다.

```
SQLite 열림: /data/worklife.sqlite · 기존 파일 이어서 씀 · 4096바이트
저장된 행: users=2 tasks=4 standups=0 attendance=1 job_runs=1
```

- 경로가 `/data/` — `DATABASE_PATH` 가 볼륨 마운트를 가리킨다
- `기존 파일 이어서 씀` + `tasks=4` — 부팅 **이전에** 쓴 데이터가 그대로 읽혔다
- `job_runs=1` — 멱등성 테이블이 살아 있어 아침 알림이 중복되지 않는다

남은 확인: 코드 변경 없이 **Redeploy 한 번** 하고 행 수가 유지되는지 보면 완전히 확정된다
(지금 로그가 재배포인지 같은 컨테이너 재시작인지는 구분되지 않는다).

### 연결된 워크스페이스

실제 워크스페이스 이름·봇 id·채널 id·멤버 목록은 **`WORKSPACE.local.md`** 에 있다.
공개 레포에 동료 식별 정보를 올리지 않으려고 분리했고, `.gitignore` 에 걸려 있다.

봇 이름은 한글이 안 돼서 영문으로 지었다 (슬랙 앱 이름 제약).
보드 채널 id 는 `.env` 의 `BOARD_CHANNEL_ID` 에 있고 `npm run channels` 로 확인한다.

### 두 채널 구조

| | DM (`Manager oh`) | `#0_daily-report` |
|---|---|---|
| 아침 리마인드(모닝 루틴 + 어제 기록) · 3문답 · 개인 명령 | ✅ | |
| 하루 시작 알림 · 개인별 요약 · 마감 현황 | | ✅ |

질문과 답변은 **DM 에서만** 오간다. 봇은 `message.im` 밖의 메시지를 답변으로 받지 않는다.

---

## 5. 남은 일

### 사용자가 해야 끝나는 것

- 🔴 **슬랙 앱 manifest 재적용 + 재설치.** `/history` 명령과 `files:write` 스코프가
  manifest 에만 있고 아직 워크스페이스에 반영되지 않았다.
  api.slack.com/apps → App Manifest 교체 → Save → Reinstall.
  안 해도 나머지는 다 돌아간다. `/history` 가 안 뜨고, `파일` 키워드가 안내 문구만 낸다.
- **Redeploy 한 번** 해서 볼륨 영속성 최종 확인 (위 "배포 상태" 참고).

### 결정 대기

- **1인 사용 시 채널 알림 생략** — 참여자가 1명이면 `1명 중 0명 완료` 가 허전하다.
  시작 알림·마감 현황을 건너뛸지 사용자 확인 필요. 한 줄이면 된다.
- **`/진단` 명령** — 지금은 저장소 상태를 부팅 로그로만 볼 수 있다.
  로그는 스크롤되면 사라지므로 언제든 DM 으로 확인할 수단이 있으면 좋겠다고 제안했으나 보류 중.

### 해결됨

- ~~done-next 시각 17:50 vs 17:30~~ — 사용자가 `/settings` 에서 **17:30** 으로 바꿨다.
  정규 퇴근 18:00 보다 30분 이르다는 본인 규칙과 맞는다.
  코드 기본값은 17:50 그대로다 — 신규 사용자에게만 적용된다.
- ~~근무시간 정책~~ — 사용자가 Railway 환경변수로 직접 조정했다.
  코드 기본값은 09:30~18:00 · 점심 12:30~14:00 · 실 근무 7시간.

### 하루 시각표 (기본값)

| 시각 | 무엇 |
|---|---|
| 09:00 | 아침 리마인드 — 모닝 루틴 알람 + 어제 기록(한 일·할 일·나에게 한 말·마일스톤) |
| 09:30 | 정규 출근 |
| 12:30~14:00 | 점심 (근무시간에서 차감) |
| 17:50 | done-next 시작 (채널 오프너 동시) — 현재 사용자는 **17:30** 으로 조정함 |
| 18:00 | 정규 퇴근 |
| 18:40 | 미제출 넛지 · 채널 마감 현황 |
| 금 17:00 | 주간보고 |

### 미구현 / 미확인

- **한글 슬래시 명령(`/출근`)을 슬랙이 받는지 미확인.**
  ASCII 별칭(`/in` `/out` `/hours`)을 manifest 에 같이 넣어 뒀고,
  거절당하면 한글 줄만 지우면 된다. DM 에 `출근` 이라고 적는 방법은 항상 동작한다.
- **컨디션(mood)** — `standups.mood` 컬럼은 있는데 질문을 안 한다.
  질문 4개는 많다고 판단했다. 켜려면 한 줄.
- **월간 회고** — 매월 1일 지난달 정산. 주간과 구조가 같아서 붙이기 쉽다.
- **다중 워크스페이스** — 지금은 봇 토큰 하나짜리 단일 워크스페이스용.
  앱 스토어 배포까지 가려면 OAuth 설치 흐름이 따로 필요하다.

### 배포할 때 잊지 말 것

🔴 **볼륨(`/data`)과 `DATABASE_PATH` 는 한 쌍이다.** 볼륨만 붙이고 변수를 안 바꾸면
컨테이너 안에 쓰다가 재배포 때 통째로 날아간다. 실제로 한 번 당했다.
부팅 로그 첫 두 줄로 확인한다 (§4 배포 상태).

⚠️ **done-next 시각에 보드 채널 멤버 전원에게 실제로 DM 이 간다.**
테스트 중이라면 미리 알리거나 채널에서 빼 둘 것.

⚠️ **로컬에서 `npm run dev` 를 띄워 두면 배포판과 둘 다 반응한다.**
같은 봇 토큰을 쓰므로 알림이 두 번 갈 수 있다. 테스트가 끝나면 로컬 프로세스를 내린다.

---

## 6. 확인용 명령

```bash
npm run smoke          # 슬랙 없이 도메인 로직 78건 검증 (action_id 중복 포함)
npm run channels       # 봇이 들어가 있는 채널 id
npm run verify-home    # 홈 탭 블록이 실제 슬랙에서 통과하는지
npm run dev            # 실제 연결 (배포판과 겹치지 않게 주의)
```

슬랙에서 확인할 때는 **시간을 기다리지 않아도 된다.**

| 확인할 것 | 치는 것 |
|---|---|
| 오늘 할 일 대화 | `/checkin` (DM 에 `할일` 이라고 적어도 됨) |
| done-next 3문답 | `/standup` (다시 쓰려면 `/standup 다시`) |
| 근무시간 계산 | `/출근` → `/퇴근` → `/근무` |
| 지난 기간 조회 | `/report 지난주` · `/history 30일` |
| 저장소 상태 | Railway Deploy Logs 첫 두 줄 |
